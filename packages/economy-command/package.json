{
  "name": "@worldgraph/economy-command",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@worldgraph/contracts": "workspace:*",
    "@worldgraph/db": "workspace:*",
    "@worldgraph/economy": "workspace:*",
    "@worldgraph/ledger": "workspace:*"
  },
  "devDependencies": {
    "tsup": "catalog:"
  }
}
